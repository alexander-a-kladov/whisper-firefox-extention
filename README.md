# whisper-firefox-extention
Firefox based client for whisper text recogintion server

start whisper.cpp server locally, like this

./build/bin/whisper-server -m models/ggml-small.bin -l ru

Add extension to Firefox in about:debuggin -> This Firefox -> Load Temporary Add-on...

Use microfon symbol near line input widgets to enter text from speech
